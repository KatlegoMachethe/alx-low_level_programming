#include <stdlib.h>
#include <stdio.h>

/**
 * main - prints the name of the file that the program is compiled from
 * Return: Always 0
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
