#include "main.h"
#include <stdio.h>

/**
 * _strlen - Returns the length of a string
 * @s: String argument
 * Return: Integer
 */

int _strlen(char *s)
{
	char pointer;

	pointer = *s;
	return (sizeof(pointer));
}
