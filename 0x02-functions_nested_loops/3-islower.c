#include "main.h"

/**
 * _islower - Checks for case
 * @c: First argument/operand
 * Return: 1 for success, otherwise 0
 */
int _islower(int c)
{
	if (c >= 65 && c <= 90)
		eturn (0);
	else
		return (1);
}
